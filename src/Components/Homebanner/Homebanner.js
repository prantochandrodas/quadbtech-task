import React from 'react';

const Homebanner = ({ bannerData }) => {
    console.log(bannerData.show.image?.original);
    return (
        <div>
            
        </div>
    );
};

export default Homebanner;